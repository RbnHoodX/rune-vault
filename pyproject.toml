[project]
name = "rune-vault"
version = "0.1.0"
description = "A cataloging system for ancient runes"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=7.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
